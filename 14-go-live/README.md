# 14-3d-text

1. Add Vercel to project
    ```js
    {
        "scripts": {
            // ...
            "deploy": "vercel --prod"
        },
    }
    ```

2. Login and deploy
    ```bash
    yarn vercel login
    yarn run deploy
    ```
